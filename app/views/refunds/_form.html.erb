<%= form_for @refund, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :transaction_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :transaction_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :product_transaction_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :product_transaction_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :quantity, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :quantity, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                refunds_path, :class => 'btn' %>
  </div>
<% end %>
